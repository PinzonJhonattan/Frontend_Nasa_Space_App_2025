import { Routes } from '@angular/router';	
import { Dashboart } from './dashboart';

const routes: Routes = [
    { path: '', component: Dashboart }
];

export class DashboartRouting {
    constructor() {}
}