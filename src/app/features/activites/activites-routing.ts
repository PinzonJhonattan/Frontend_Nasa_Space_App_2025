import { Activites } from './activites';
import { Routes } from '@angular/router';

const activitesRoutes: Routes = [
    {
        path: 'activites',
        component: Activites
    }
];

export default activitesRoutes;